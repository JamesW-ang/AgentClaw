# ============================================================================
# AgentClaw CI Minimal Dependencies - 仅用于 CI 测试
# ============================================================================

# 核心依赖（测试必需）
fastapi==0.115.9
# starlette==0.46.2  # 由 fastapi 自动管理
pydantic==2.10.6
python-dotenv==1.1.0
psutil==6.1.0

# HTTP 客户端
httpx==0.28.1

# LLM 客户端（测试必需）
openai>=1.50.0

# LangGraph（部分测试需要）
langgraph==0.3.5

# 测试框架
pytest>=7.0
pytest-cov>=4.0
pytest-timeout>=2.0
pytest-mock>=3.0

# 代码质量
ruff>=0.1.0

# 代码覆盖率
coverage>=7.0
