name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Cache pip packages
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements-ci.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          pip install -r requirements-ci.txt

      - name: Run linter
        run: |
          pip install ruff
          ruff check . || true

      - name: Run tests
        run: |
          pytest test/ -v --tb=short --ignore=test/test_integration.py --ignore=test/test_react_agent.py || true

      - name: Generate coverage
        run: |
          pytest test/ --cov=. --cov-report=xml --cov-report=html --ignore=test/test_integration.py --ignore=test/test_react_agent.py || true
